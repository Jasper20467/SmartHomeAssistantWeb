# Docker Push Configuration
# 此檔案包含 Docker 推送腳本的設定

# Docker Hub 設定
DOCKER_USERNAME=popo510691
DOCKER_REGISTRY=docker.io

# 映像名稱設定
FRONTEND_IMAGE=homeassistant.frontend
BACKEND_IMAGE=homeassistant.backend
LINEBOT_IMAGE=homeassistant.linebot

# Dockerfile 路徑
FRONTEND_DOCKERFILE=docker/frontend.Dockerfile
BACKEND_DOCKERFILE=docker/backend.Dockerfile
LINEBOT_DOCKERFILE=docker/linebot.Dockerfile

# 建置上下文
FRONTEND_CONTEXT=./frontend
BACKEND_CONTEXT=./backend
LINEBOT_CONTEXT=.

# 預設版本號格式 (使用 PowerShell 日期格式)
# yyyy.MM.dd.HHmm = 年.月.日.時分 (例如: 2025.07.06.1200)
DEFAULT_VERSION_FORMAT=yyyy.MM.dd.HHmm

# 自動更新的配置檔案
AWS_EC2_CONFIG=scripts/DeployOn_AWS_Ec2/docker-compose_fromHub.yml
AZURE_CONFIG=scripts/DeployOn_Azure_ContainerApps/azure-docker-compose.yml

# 建置選項
ENABLE_BUILDKIT=true
DOCKER_BUILDKIT=1

# 推送選項
PUSH_LATEST_TAG=true
CLEANUP_OLD_IMAGES=false

# 輸出選項
VERBOSE_OUTPUT=true
COLORED_OUTPUT=true

# 備份選項
CREATE_CONFIG_BACKUP=true
BACKUP_SUFFIX=.backup
